

<p id="notice"><%= notice %></p>

<h1>訂單列表</h1>

<table>
  <thead>
    <tr>
      <th>訂單ID</th>
      <th>商店名稱</th>
      <th>使用者</th>
      <th>狀態</th>
      <th>拒絕原因</th>
      <th>訂單內容</th>
      <th>訂單建立時間</th>
      <th>訂單完成時間</th>
      <th>總費用</th>
      <th>遞送費用</th>
      <th>商品費用</th>
      <th>小費</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.id %></td>
        <td><%= order.store_name %></td>
        <td><%= order.user_id %></td>
        <td><%= order.status %></td>
        <td><%= order.reject_reason %></td>
        <td><%= order.content %></td>
        <td><%= order.created_at %></td>
        <td><%= order.finished_at %></td>
        <td><%= order.order_fee %></td>
        <td><%= order.delivery_fee %></td>
        <td><%= order.items_fee %></td>
        <td><%= order.tip %></td>
        <td><%= link_to 'Show', order %></td>
        <td><%= link_to 'Edit', edit_order_path(order) %></td>
        <td><%= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '手動增加訂單', new_order_path %>
<p></p>
<p>訂單狀態：0:新單、1:接受、2:拒絕、4:完成、5:失效</p>

